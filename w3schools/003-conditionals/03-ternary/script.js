{
	let age = 21;
	let text = age < 18 ? "Minor" : "Adult";
	console.log(text);
}

{
	let isMember = true;
	let discount = isMember ? 0.2 : 0;
	console.log(discount);
}

{
	let isMember = false;
	let discount = isMember ? 0.2 : 0;
	console.log(discount);
}
